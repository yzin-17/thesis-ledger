# 全站 UI 与交互统一规格

任务标识：`ui-interaction-consistency`  
状态：实施中；用户已授权开始实施。  
配套文档：[实施任务](../tasks/2026-09-08-ui-interaction-consistency.md)

## 背景与目标

全站已有共享组件和主题变量，但页面标题、布局间距、容器边界及操作反馈存在不同实现。本主题统一视觉语言和操作习惯，保留内容类型需要的信息密度。用户已明确要求保留装饰性英文副标题。

桌面页面共享同一套视觉与交互契约，因此使用一个规划单元，分页面独立验收。移动端作为独立验收批次，复用语义及视觉层级，保留原生操作习惯。

## 现状与范围

本轮完成路由与代表性源码盘点，未执行全量浏览器交互检查。下面列出检查范围，不表示各页面已经完成体验审计。

| 页面 | 必须覆盖的内容 |
| --- | --- |
| 投资组合 | 组合概览、持仓、交易、模式切换、行情详情 |
| 账户数据 | 账户管理、持仓、成交、现金、定期入金、对账、审计及编辑侧栏 |
| 风险中心 | 总览、规则、事件、通知及规则编辑 |
| 收益分析 | 账户与时间筛选、指标、趋势、目标编辑 |
| 策略实验 | 策略库、编辑、回测设置、任务、结果 |
| 投资复盘 | 候选交易、填写、结果、证据及高级编辑 |
| 研究助手 | 研究列表、新建、运行详情、结果与证据链 |
| 数据与自动化 | 数据源、连接测试、自动化、诊断及编辑侧栏 |
| 市场数据 | 标的目录、同步、数据源与策略配置 |
| 全局入口 | 导航、主题、首次引导、旧链接跳转、确认弹窗和消息提示 |
| 移动端 | 投资组合、风险事件、刷新、模式及主题切换 |

源码可确认的切入点包括：“账户数据”导航与“资产录入”页标题不同；页头和 Tab 间距实现不同；传统样式与原子类并存；部分页面使用独立消息状态。样式并存本身不等于视觉缺陷，须结合运行证据判断。

## 约束与非目标

- 延续现有 shadcn/Base UI、主题变量和共享组件体系，优先原子类、组件变体及组合，不新增页面级覆盖规则作为常规方案。
- 保留既有业务、接口、金额口径、币种、真实与模拟模式、权限及功能开放状态；本主题不扩展业务功能。
- 保留工作区未提交改动，尤其共享 Sheet 和策略实验；每批开始前重新确认相关文件当前状态。
- 本主题不更新其他任务完成状态、不发布、不提交代码。
- 对超过尺寸或复杂度门限的文件，新增职责须收敛至明确组件；不得提高门限或机械拆分。

## 设计方案

### 标题与文案

一级页面采用“装饰性英文副标题 → 中文主标题 → 可选说明”。英文简短、全大写，统一字距、弱化颜色和位置。中文标题为主要层级。Tab、卡片和表单内部使用相应标题层级，不重复整套页头。

导航名称与页标题一致；投资组合中的总资产作为指标呈现，确保页面仍有清晰的名称。说明描述用户可以完成的任务，版本、Schema、引擎等细节放在相关配置或结果附近。

以下为可执行的初始命名与尺寸提案，须在代表页面核验后将最终值回写本节，再迁移其余页面。

| 中文标题 | 英文副标题 |
| --- | --- |
| 投资组合 | PORTFOLIO |
| 账户数据 | ACCOUNT DATA |
| 风险中心 | RISK CENTER |
| 收益分析 | PERFORMANCE |
| 策略实验 | STRATEGY LAB |
| 投资复盘 | INVESTMENT JOURNAL |
| 研究助手 | RESEARCH ASSISTANT |
| 数据与自动化 | DATA & AUTOMATION |
| 市场数据 | MARKET DATA |

初始桌面排版：英文 12px、字重 600、字距 0.16em；页面标题 32px、字重 600、行高 1.25；说明 14px、行高 1.6。英文至中文间距 8px，中文至说明 12px，页头至正文 24px。窄窗口标题使用 28px，操作区允许自然换行。模块标题 18px、侧栏标题 20px。优先映射现有样式能力；如实际字体、内容密度需要调整，试点统一修订数值，不逐页另定标准。

### 边框、分隔线与容器

| 场景 | 规则 |
| --- | --- |
| 页面与模块 | 优先留白；同组内容的明确分区可使用单条分隔线 |
| 独立卡片 | 独立操作或成组数据可使用一层外框；内部优先留白、浅背景或分隔线 |
| 表格与列表 | 表头和行分隔统一；不默认增加每个单元格的竖线；悬停与选中语义区分 |
| 表单控件 | 复用统一边框、圆角与高度；聚焦和错误不能仅靠轻微颜色差异 |
| 按钮 | 实心表示主要操作，描边表示次要操作，轻量按钮用于低强调操作；每个操作区明确主要动作 |
| 侧栏与弹窗 | 使用共享边界、遮罩和阴影；内部仅在固定操作区与滚动内容之间需要时分隔 |

边框默认 1px、使用语义化颜色；常规控件与容器初始圆角均采用现有 6px 级别；常规静态卡片不使用浮层阴影。每条线应能解释其分组、交互或层级用途。嵌套容器若保留多层外框，须在问题清单说明独立语义。浅色和深色采用相同语义，分别验证可辨识性。

### 交互契约

- 表单：统一字段说明和就近错误；提交中防重复提交；失败保留输入；成功等待相关数据刷新后关闭或返回。写入成功但刷新失败时，明确提示刷新失败，提供重新读取入口，避免诱导再次写入。
- 关闭：有未保存修改的编辑界面，关闭按钮、Esc、遮罩和页面离开遵循一致提醒规则；无修改时可直接关闭；关闭后焦点返回触发入口或其合理替代位置。
- 侧栏：初始采用 480px 简单表单、640px 复杂表单、960px 宽详情三级上限，实际宽度不超过视口减 16px。保持一个清晰滚动区域，底部操作不遮挡最后一个字段。跨层选择器与确认框须验证焦点和遮挡。
- 搜索与筛选：清楚展示当前范围及重置动作；同页面打开再关闭详情保留筛选和位置；账户或模式变化时不能展示上一范围的数据为当前结果。跨页面保留范围沿用现有契约，有变化先记录于规格。
- 反馈：短暂成功使用 Toast；字段错误放在字段附近；持续的加载失败或过期状态放在对应区域并提供恢复动作。长任务必须能看见运行、完成和失败状态。
- 空状态：区分首次无数据、筛选无结果、依赖未配置和请求失败，不把错误或未知值渲染为零资产。
- 展示：同类金额、日期和比例统一格式，币种清晰，缺失值与零区分；颜色同时配合数值、文字或图标表达。
- 可访问性：可见焦点、图标按钮可访问名称、合理焦点顺序；标签仅作说明，遵守项目禁止默认标签激活控件的规则。

### 研究助手空状态与主从工作区

研究助手在“全部”范围确认没有历史任务时，使用一个工作区级首次引导，不同时渲染空任务列表和空详情。首次引导只保留一个“新建研究”主操作，并展示最多三个可预填且仍可编辑的问题模板。标题区在空状态不重复提供同一创建操作；任务存在或用户查看非“全部”筛选时恢复标题区创建入口和主从工作区。

研究列表保留状态筛选，但常用桌面宽度不得因选项间距产生可见横向滚动条；窄窗口仍允许横向滚动。页面只保留标题区的共享刷新入口，刷新同时更新列表和当前详情。Provider 不可执行时，状态旁提供进入“数据与自动化”的明确配置入口；能力检查失败继续按错误状态表达，不将其误报为未配置。

首次加载使用与最终主从结构一致的骨架。列表首次读取失败且没有缓存数据时，展示单一工作区错误状态和就近重试，不同时出现“开始第一次研究”的误导信息。非“全部”筛选为空时保留筛选导航，并明确提示切换状态或新建研究。

## 对外行为、状态及兼容性

对外变化集中于标题、布局、边界与操作反馈，不修改服务端接口或持久化模型。旧入口跳转、账户及模式隔离、行情详情与复盘链接须回归。原有禁用入口继续保留原因说明。共享能力仅承载稳定表现与通用交互，业务编排由各 feature 持有。

## 测试策略

先形成全量入口与状态矩阵，再逐批验证实际页面。每个入口记录位置、检查状态、问题编号、视口、主题和证据；状态不适用时说明理由，环境阻塞时记录具体依赖，不标记通过。

桌面至少检查 1440×900、1024×768、390×844；全部一级页面覆盖浅深主题，全部侧栏与弹窗检查打开、滚动、关闭及焦点。状态通过受控测试数据或请求模拟验证，不为制造错误修改真实业务数据。移动端在可用设备或模拟器验证，浏览器窄窗口不替代原生验收。

复用现有 UI、路由、账户、风险、收益、研究与复盘测试入口；对异步保存、失败恢复和范围切换新增必要的行为回归。纯视觉数值以实际渲染核验，不新增仅匹配 className 字符串的测试。类型、构建和静态检查不替代运行验收。涉及原生依赖或构建配置时执行对应 Native compile。

## 风险与备选方案

共享样式可能影响所有调用方，使用兼容扩展、分批迁移、最终清理的顺序。样式局部统一即可满足时，不扩大组件改造。存量大文件只提取本主题新增或变化职责。运行环境缺少数据或外部服务时，模拟证据与真实环境证据分别记录。

## 未决问题

### 阻塞问题

无阻塞规划的问题。用户已授权实施，按任务依赖与验证证据推进。

### 非阻塞假设

- 标题命名、排版及侧栏尺寸采用本规格初始提案；试点根据实际渲染统一校准，影响限定为视觉参数，须回写规格。
- “所有页面”默认包含移动端当前两页；保留移动端只读能力，按独立任务验收。

## 验收标准

- AC1：全部一级页面、Tab、侧栏、弹窗和全局入口具有检查记录；正常及适用的异常状态均有证据或明确阻塞说明；有未解决阻塞时不得宣称全量验收通过。
- AC2：九个桌面一级页面采用统一英文副标题、中文标题及说明层级；导航与页标题含义一致，尺寸和间距符合试点定稿值。
- AC3：各页面边框、分隔线、背景、圆角和阴影符合场景规则，例外有明确语义；浅深主题和三个目标视口无内容遮挡。
- AC4：所有编辑入口提交中防重复、失败保留输入、成功刷新后返回；刷新失败不会诱导重复写入；有修改的关闭行为一致。
- AC5：所有适用页面区分空、失败、过期和加载状态；反馈就近可恢复，账户与模式切换无错误范围内容，详情往返保留原范围。
- AC6：控件、数字格式、按钮优先级及焦点行为统一；标签交互符合项目约束，菜单和浮层不遮挡关键操作。
- AC7：策略实验、账户数据和投资组合的代表流程通过运行验收，最终规则已同步回规格，再推广其余页面。
- AC8：移动端两页完成同类文案、数据及状态语义统一，在设备或模拟器记录验证结果，保留原生交互及只读范围。
- AC9：业务接口、金额口径、旧入口与功能开放状态保持兼容；各批完成相应测试，最终覆盖表区分静态、浏览器、原生与外部依赖证据。
- AC10：桌面端所有带行级操作的表格使用共享操作列语义组件；横向滚动时操作表头与单元格固定在最右侧，背景不透出滚动内容并保留清晰左边界；纯只读表格不增加固定列。
- AC11：策略库不重复拼接已包含首个标的的描述，版本数据时点使用中文本地时间格式，已完成回测使用克制的非 primary Badge。
- AC12：Desktop 所有用户可见的时点、创建、更新、运行、检查和证据时间均不直接展示 ISO 8601 的 `T`／`Z` 文本；常规时点使用中文本地时间，业务日期区间保持 `YYYY-MM-DD`，纯日期、月份、表单值、API、导出、JSON 与日志不改变语义或存储格式。
- AC13：研究助手在无历史任务时只显示一个工作区级首次引导和一个主要创建入口；问题模板会预填新建侧栏且不自动提交；任务存在后恢复主从布局。页面只有一个刷新入口，常用桌面宽度的状态筛选无可见横向滚动条，Provider 不可执行时可直接进入配置页；首次读取失败不会同时显示创建引导。


## Drawer 补充统一（2026-09-08）

针对用户截图中的标题留白和表单外框差异，逐项复查所有 Sheet 调用方及内嵌账户表单。统一外侧留白 24px、标题说明间隔 8px、标题区至正文 24px；标准页头不带分隔线。普通表单使用开放布局，不套整块装饰外框；输入、独立数据块及底部操作区保留有含义的边框。底部操作区使用单条顶线、16px 上内边距，按钮靠右并允许换行。保留原有滚动、草稿与业务行为。

验收须逐项记录所有 Drawer 的源码覆盖，并实测用户截图对应的创建账户和录入成交，以及不同内容结构的代表侧栏；新增防回退检查，禁止遗留页头与整块表单外框重新进入 Drawer。
