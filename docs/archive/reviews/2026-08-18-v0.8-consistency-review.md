# V0.8 一致性 Review

专业 Provider 只通过 `MarketDataProvider`、PIT Financial 和 ProviderConfig Contract 接入；Tushare/JQData 适配器位于 `services/dsa-adapter`，核心 Market/Portfolio/Risk 不导入供应商 SDK。配置校验、健康、额度、凭证状态和统一 Quote/Bar Contract 测试已通过。

真实供应商 token、授权额度和人工主备切换仍需部署环境。
