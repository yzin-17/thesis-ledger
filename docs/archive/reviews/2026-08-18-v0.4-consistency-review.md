# V0.4 一致性 Review

RiskRule Schema → RiskService Evaluation Context → RiskEvent → NotificationService → Delivery/Audit 的链路已统一，事件保留 ruleVersion、marketTime、dataQuality 和 traceId。规则修改、启停、测试和通知失败均有审计；AI 只消费事件，不重复计算规则。domain/server 回归覆盖固定价、成本、指标、筹码、集中度、相关性、静默和通知重试。

Risk Center UI 已实现规则、测试、历史和通知视图；浏览器人工新建→触发→查看流程仍待可运行环境验收。
