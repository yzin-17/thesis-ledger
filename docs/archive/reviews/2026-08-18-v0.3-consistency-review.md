# V0.3 一致性 Review

LedgerEvent 是交易与现金事实源；PortfolioService、ImportService 和迁移命令都经 LedgerService 写入，Position 只由 AVG/FIFO 投影重建。Snapshot 使用行情和 Ledger 现金，Performance 提供 Security/Account/Portfolio 三层结果、TTWROR/XIRR、目标配置和再平衡差额。server/domain 测试覆盖幂等、迁移、现金、公司行动和三层聚合。

代码搜索允许的 `position.create/delete` 仅出现在 Ledger projection rebuild；其余业务写入口均拒绝直接 Position 写入。真实迁移数据库和浏览器指标验收需 staging/人工资源。
