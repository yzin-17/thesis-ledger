# V0.9 一致性 Review

AutomationJob/Run 使用统一 cron/timezone、交易日检查、Redis 锁、retry/backoff 和 history。盘前、盘中、收盘、日报和周报工作流只调用 Market、Performance、Risk、ProviderHealth 与 Journal/AI 的既有服务，不复制一套风控算法。workflow tests、server build/test 已通过；完整交易日 Compose E2E 仍需外部数据库/Redis。
