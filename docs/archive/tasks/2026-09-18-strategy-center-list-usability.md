# 策略中心列表可读性与交互优化实施任务

- 状态：已完成
- Spec：[策略中心列表可读性与交互优化](../../specs/2026-09-18-strategy-center-list-usability.md)
- 上位任务：[策略中心统一交互与回测结果体验优化](2026-09-17-strategy-center-ux-consolidation.md)

## 1. 任务清单

- [x] T01：核对现有列表接口、路由、组件与脏工作树
  - 覆盖：AC-L01—AC-L10。
  - 结论：现有接口可提供版本、来源、状态、阶段、错误摘要、AI 调用数和回测次数；不提供列表级候选总数与回测指标摘要。本轮不得虚构缺失统计。
  - 边界：策略中心相关文件已有未提交实现，按用户现有工作增量修改，不重置或覆盖无关改动。

- [x] T02：收敛页头与策略库工具栏、列结构和操作
  - 覆盖：AC-L01—AC-L03、AC-L08—AC-L09。
  - 完成条件：移除眉题和重复说明；策略库展示明确最新版本、最近回测与回测主操作；无强制桌面横向滚动。
  - 验证：展示纯函数测试、策略中心界面契约测试、Desktop typecheck。
  - 实施证据：页头移除英文眉题与常驻说明；策略库合并搜索和新建入口，列收敛为策略、版本、最近回测、更新时间和静态窄操作列，回测动作绑定当前所示最新版本。重复搜索框、工具栏及回测状态/结果展示提取到独立列表展示组件；既有大文件分别从 470/380/396 行收敛至 468/377/382 行，未继续增加技术债 ratchet。

- [x] T03：优化回测任务分组、状态进度与结果摘要
  - 覆盖：AC-L03—AC-L05、AC-L07—AC-L09。
  - 完成条件：分组状态数量来自当前匹配任务；失败摘要、完成入口和未完成占位语义互斥；操作列不固定。
  - 验证：分组汇总纯函数测试、界面契约测试、现有导航与详情测试。
  - 实施证据：实验组按当前匹配任务汇总中文状态数量；用户任务和子任务分别显示业务区间、真实进度、失败摘要或结果入口；展开集合写入 `expanded` 查询参数，详情返回可恢复。

- [x] T04：优化 AI 实验来源、状态阶段与进展表达
  - 覆盖：AC-L06—AC-L09。
  - 完成条件：列表无 UUID 和模型路径；来源、状态、阶段和实际消耗使用中文业务表达；改名和克隆保持可用。
  - 验证：来源与状态映射纯函数测试、界面契约测试、现有实验列表测试。
  - 实施证据：名称下改为标的或来源策略；状态与阶段独立显示；进展只消费实际 AI 调用数、回测次数和已选择候选事实；完整模型路径保留在详情，不再占用列表列宽。

- [x] T05：包级验证与浏览器验收
  - 覆盖：AC-L10。
  - 依赖：T02—T04。
  - 验证顺序：定向测试 → Desktop typecheck → Desktop build → 目标浏览器常见桌面宽度、窄屏、刷新、分页和展开检查。
  - 证据边界：若目标运行态未与当前源码同步，浏览器项保持未完成，不以构建产物替代。
  - 自动化证据：`pnpm --filter @thesis-ledger/desktop test` 通过，62 个测试文件、415 个断言全部成功；`pnpm --filter @thesis-ledger/desktop build` 通过；`git diff --check` 通过。构建保留既有大 chunk 警告，不影响本轮功能验收。
  - 浏览器证据：Vite 当前源码连接本地实际 Server API，在 1280×900、1024×768、768×768 三档视口检查策略库、回测任务和 AI 实验。文档与表格 `scrollWidth` 未超过 `clientWidth`；最终 1024 宽三张表分别为 718/718、707/707、707/707，真实列表可见来源、状态阶段、失败摘要及实际调用/回测次数。回测实验组展开后 URL 保存 `expanded`，当前页任务数与状态数量一致。修复 Base UI 链接式按钮语义后，重启开发服务并复验三页控制台无错误。
  - 环境边界：浏览器证据证明当前工作树前端源码与本地实际 API 的消费和布局，不证明已安装 Desktop 包或 Docker 镜像已更新。本轮没有运行 Docker 更新入口。

## 2. 规划预检

- 结论：Ready。
- 覆盖：10 条验收标准均有实现与验证责任；缺失列表统计采用明确降级，不构成阻塞问题。
- 依赖：本轮只消费现有读取契约，不改变 Server、数据库、队列或 AI 执行状态机。
- 风险：目标文件属于未提交在建实现；验证需明确基于当前工作树，不能声称形成独立提交。

## 3. 最终一致性 Review

- [x] Spec 的全部验收断言均有明确实现与适当验证
- [x] 所有已勾选任务满足自身完成条件且证据仍有效
- [x] 必要浏览器与目标运行态门禁通过或保持明确未完成
- [x] 用户可见枚举、时间、未知结果与操作语义一致
- [x] 未覆盖用户现有改动，未扩大到 Server 与执行状态机
- [x] 文档、测试与当前工作树实现状态一致

### Review 结论

- 结论：通过；本轮列表可读性与交互优化范围完成。
- 已确认的问题及处理：全局 sticky 操作列门禁与本轮静态窄列冲突，已改为显式 `data-table-action-static` 例外，其他页面默认门禁不变；Base UI 链接组合警告已通过 `nativeButton={false}` 消除。
- 尚未通过的门禁：无本轮必要门禁。安装包与 Docker 镜像更新不属于本轮验收声明。
- 遗留风险：实验列表接口仍不提供候选总数和回测指标摘要；界面按 Spec 保持真实缺省表达，未构造统计。
- 提交状态：当前工作树包含大量既有未提交改动；本轮未获提交授权，不创建提交。
