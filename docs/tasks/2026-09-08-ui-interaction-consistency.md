# 全站 UI 与交互统一实施任务

对应规格：[全站 UI 与交互统一](../specs/2026-09-08-ui-interaction-consistency.md)  
任务标识：`ui-interaction-consistency`  
状态：实施中；基线已建立，视觉与交互分步验证。

## 执行边界

用户已授权实施本主题。开始实施前重新读取相关工作区状态，衔接共享 Sheet 与策略实验现有修改。复用 shadcn/Base UI；新增或修改组件前按 shadcn skill 检查组件文档。涉及工具函数选型时使用 recommend skill。修改共享职责或依赖时检查边界门禁，避免引入无所有权的共享层。

## 当前进展

[检查记录](../reviews/2026-09-08-ui-interaction-consistency.md)已记录实现、浏览器证据与阻塞。九页页头、边框、Tab、Sheet 尺寸和移动端标题已迁移；常用编辑入口接入草稿保护，并修复策略刷新反馈与空配置目标编辑。桌面 170 项测试、构建，移动端 7 项测试和类型检查已通过；iOS 原生编译通过但启动被动态库符号不匹配阻塞。全量次级入口和写入恢复尚未验收，任务按完整完成条件保持未勾选。

共享规范定稿后，各页视觉迁移已完成；下列任务的依赖作为完整交互验收门槛继续保留，不能据视觉迁移提前勾选。

## 任务

- [ ] T1：建立全量页面与交互基线
  - 覆盖验收标准：AC1、AC9。
  - 依赖：无。
  - 涉及范围：`apps/desktop/src/app/routes.tsx`、`features/`、全局导航与引导、`apps/mobile/src/`。
  - 完成条件：在 `docs/reviews/` 建立本主题检查报告；逐项列出规格范围内所有入口、Tab、浮层和适用状态，记录截图、视口、主题、问题位置及优先级。阻断、误操作或数据丢失列最高优先级，其次高频交互差异，再次视觉细节。
  - 验证方式：实际遍历入口，路由与组件调用清单交叉核对覆盖率；区分源码观察、实际复现及环境阻塞。不得将未访问入口标记完成。

- [ ] T2：完成三个代表页面试点并定稿共享规范
  - 覆盖验收标准：AC2、AC3、AC4、AC5、AC6、AC7、AC9。
  - 依赖：T1 的入口清单与实际视觉基线已建立；异常状态全量核验在各页面批次继续，不阻断已复现的视觉修复。
  - 涉及范围：`components/ui/`、`styles/`、`features/shared/`、策略实验、账户数据和投资组合。
  - 完成条件：实现兼容的页头、容器及控件规则；完成代表页面全部适用入口，校准字号、间距与侧栏档位并回写规格。共享组件不接收 feature 专属编排。未迁移页面仍可正常使用。
  - 验证方式：实际检查策略编辑与回测设置、创建首个账户并返回列表、现金录入、持仓至行情详情、交易至复盘；失败保留输入、写入后刷新失败和切换账户有行为验证；复用账户、交易、策略及 UI 测试，执行桌面类型检查和构建。

- [ ] T3：迁移风险中心与收益分析
  - 覆盖验收标准：AC2、AC3、AC4、AC5、AC6、AC9。
  - 依赖：T2。
  - 涉及范围：`features/risk/`、`features/performance/`。
  - 完成条件：全部 Tab、规则与目标编辑、扫描与通知反馈、时间筛选及图表采用定稿规则；真实与模拟模式含义不变。
  - 验证方式：实际走通规则编辑、扫描状态、通知依赖缺失、目标保存、时间及账户切换；运行风险和收益现有 UI 测试及桌面类型检查，记录浅深主题和窄窗口证据。

- [ ] T4：迁移投资复盘与研究助手
  - 覆盖验收标准：AC2、AC3、AC4、AC5、AC6、AC9。
  - 依赖：T2。
  - 涉及范围：`features/journal/`、`features/ai/`。
  - 完成条件：列表、编辑、结果与证据浮层均符合统一规范；详情返回保留上下文，长内容可阅读、可滚动，研究任务状态明确。
  - 验证方式：走通交易跳转复盘、保存失败恢复、证据编辑、新建研究与结果阅读；运行复盘和研究 UI 测试及类型检查，检查长文本、无候选交易和依赖未配置状态。真实研究调用与受控模拟分别记录。

- [ ] T5：迁移数据与自动化及市场数据
  - 覆盖验收标准：AC2、AC3、AC4、AC5、AC6、AC9。
  - 依赖：T2。
  - 涉及范围：`features/providers/`、`features/market-data/`。
  - 完成条件：数据源编辑、测试、自动化、诊断、目录同步和策略配置使用统一状态反馈与边界样式；原始技术错误转换为可行动说明，必要诊断信息保留在对应区域。
  - 验证方式：测试配置保存、连接失败、同步运行与失败、分页与筛选；运行配置及自动化 UI 测试和类型检查；外部发送、真实任务执行使用受控环境，环境缺失记录阻塞。

- [ ] T6：统一全局入口并清理迁移兼容样式
  - 覆盖验收标准：AC1、AC2、AC3、AC5、AC6、AC9。
  - 依赖：T3、T4、T5。
  - 涉及范围：应用壳、导航、首次引导、旧跳转、共享浮层、主题与全局样式。
  - 完成条件：全局入口采用同一规则；确认所有调用方已迁移后删除本主题被替代的兼容样式，不删除仍有有效调用的样式；不改变禁用入口状态。
  - 验证方式：执行旧链接与导航测试、窄窗口导航、主题切换、键盘遍历、浮层焦点恢复及遮挡检查；全局搜索确认清理项无剩余调用。

- [ ] T7：完成移动端一致性适配
  - 覆盖验收标准：AC3、AC5、AC6、AC8、AC9。
  - 依赖：T2。
  - 涉及范围：`apps/mobile/src/App.tsx`、两类页面、主题及样式。
  - 完成条件：同类数据与状态用语、颜色语义和标题层级一致；保留移动端布局、触控和只读能力，不引入桌面组件依赖。
  - 验证方式：设备或模拟器检查两页、模式、刷新、主题、空数据和失败恢复；记录平台、视口与证据。修改原生依赖或配置时运行对应 Native compile；环境不可用时保持原生验收未完成。

- [ ] T8：完成全站回归与证据收口
  - 覆盖验收标准：AC1、AC2、AC3、AC4、AC5、AC6、AC7、AC8、AC9。
  - 依赖：T6、T7。
  - 涉及范围：本主题规格、任务、检查报告及相关测试。
  - 完成条件：覆盖表全部有结论，所有必需验收通过，无未解决运行阻塞；每个任务完成状态可追溯到证据；清晰记录未提交状态。
  - 验证方式：执行桌面测试与构建、`pnpm accessibility:check`、受影响的复杂度和架构门禁；检查全部页面浅深主题及目标视口。已有失败区分基线与本次引入，但不得作为已通过证据。静态结果不替代浏览器和原生验收。

- [ ] T9：统一桌面端操作列固定行为并修复策略库展示
  - 覆盖验收标准：AC3、AC6、AC9、AC10、AC11。
  - 依赖：T2 的桌面表格与策略实验试点；不改变既有未完成任务状态。
  - 涉及范围：`apps/desktop/src/features/shared/StickyTableActions.tsx`、策略库、投资组合、账户数据、Provider、收益分析及对应 UI 契约测试。
  - 完成条件：完成 Desktop 全部真实操作列表审计；操作表格的表头和单元格迁移到共享 sticky 语义组件，组件具备右固定、不透底、左边界和层级契约；只读表格明确记录排除理由；策略库三处展示问题修复，不暴露内部标识。
  - 验证方式：运行操作列静态门禁、受影响 UI 测试、Desktop 类型检查和差异检查；在窄视口制造横向滚动，验证策略、投资组合、账户数据和 Provider 等可用数据页的操作列坐标保持最右侧；无数据页用源码审计和测试记录。

- [x] T10：审计并统一 Desktop 用户可见日期时间展示
  - 覆盖验收标准：AC12。
  - 依赖：无。
  - 涉及范围：`apps/desktop/src` 的用户可见页面、共享展示 helper、策略回测和研究证据链，以及 Desktop 回归测试与检查记录。
  - 完成条件：全量区分用户可见时间、业务纯日期／月份、表单值和机器可读字段；用户可见时点通过共享 formatter 展示为中文本地时间，不保留 `T`／`Z`；回测日期区间保持 `YYYY-MM-DD`；无效或缺失值使用页面语义的占位。
  - 验证方式：运行全量时间字段源码审计、formatter 行为测试和直接渲染防回退检查；执行 Desktop 全测试、类型检查和 `git diff --check`；在回测设置／结果及至少三个含日期数据的页面检查实际 DOM 与截图。
  - 验证证据：2026-09-08 审计 59 个日期时间相关源码路径；修复策略回测的 Schema／结果数据时点、日期区间，以及研究证据链的观察／市场／可用／抓取时点。Desktop 27 个测试文件、186 项通过；类型检查与 `git diff --check` 通过。浏览器在策略实验、投资组合、数据与自动化、风险中心实测，无原始 ISO 泄漏；详见检查记录“日期时间展示专项”。

- [ ] T11：收敛研究助手首屏空状态与主从交互
  - 覆盖验收标准：AC3、AC5、AC6、AC9、AC13。
  - 依赖：已定稿的 `PageHeader`、`Empty`、`Tabs` 与侧栏规范；不依赖 T4 中投资复盘和真实 Provider 结果链的未完成验收。
  - 涉及范围：`apps/desktop/src/features/ai/`、研究助手 UI 契约测试及本主题检查记录。
  - 完成条件：无历史任务时只保留一个工作区级引导和一个主创建入口；三个问题模板可预填新建侧栏；标题区复用共享刷新，任务列表不重复刷新；常用桌面宽度的五个状态筛选不出现可见横向滚动条；Provider 不可执行时提供配置入口；加载、首次读取失败、筛选为空和已有任务分别呈现正确结构，研究业务接口与只读边界不变。
  - 验证方式：新增并运行研究助手 UI 契约测试，执行 Desktop 类型检查与构建、`git diff --check`；在 1440×900、1024×768 和 390×844 检查浅深主题、空状态、筛选、Provider 配置入口、问题模板预填及侧栏关闭焦点。
  - 当前证据：首次空状态、单一主操作、三个问题模板、Provider 配置入口、浅深主题和 1440×900／1024×768／390×844 响应式布局已通过浏览器复验；模板会预填且关闭侧栏后焦点返回模板入口。Desktop 29 个测试文件、203 项通过，类型检查、生产构建、修改文件 ESLint、13 项无障碍静态检查与 `git diff --check` 通过。当前服务无研究任务，已有任务和筛选结果的真实数据浏览器态尚未复验，因此任务保持未勾选；源码和 UI 契约测试已覆盖对应分支。

## 验收映射

| 验收标准 | 对应任务 |
| --- | --- |
| AC1 | T1、T6、T8 |
| AC2 | T2、T3、T4、T5、T6、T8 |
| AC3 | T2、T3、T4、T5、T6、T7、T8 |
| AC4 | T2、T3、T4、T5、T8 |
| AC5 | T2、T3、T4、T5、T6、T7、T8 |
| AC6 | T2、T3、T4、T5、T6、T7、T8 |
| AC7 | T2、T8 |
| AC8 | T7、T8 |
| AC9 | T1、T2、T3、T4、T5、T6、T7、T8 |
| AC10 | T9、T8 |
| AC11 | T9、T8 |
| AC12 | T10 |
| AC13 | T11 |

## 规划审查

- 结论：规划具备一致性，包含已声明的非阻塞视觉参数假设；用户已授权实施。
- 覆盖：AC1 至 AC13 均有任务，任务引用均有效；T9 独立覆盖桌面操作列固定与策略库展示修复，T10 独立覆盖用户可见日期时间展示，T11 独立覆盖研究助手首屏空状态和主从交互收敛。
- 依赖：先盘点，再完成兼容试点；各页面批次消费试点定稿规则；T9 依赖桌面表格和策略试点的既有边界，不改变其余未完成任务状态；兼容清理依赖桌面全部迁移；最终验收包括移动端。
- 未决问题：无阻塞规划的问题；视觉参数在试点统一校准，移动端按规格默认范围执行。
- 当前证据：详见检查记录中的桌面浏览器、自动化测试和 iOS 原生编译／启动结果。
- 遗留风险：共享组件与策略已有未提交修改，实施前重新对齐；本地服务停止与 iOS 动态库不匹配限制运行验收。

## 最终一致性审查

- [ ] 全部验收标准均有实现与适当证据。
- [ ] 所有已勾选任务的依赖已完成。
- [ ] 规格、定稿视觉参数、页面实现和覆盖表一致。
- [ ] 原有业务与接口保持兼容，无范围外修改。
- [ ] 测试、浏览器、原生及外部依赖结果分别记录。
- [ ] 文档同步完成，提交状态明确。

结论：已实施共享视觉及常用编辑交互，静态验证通过；运行验收存在已记录阻塞与未遍历入口，尚未满足最终完成条件。


## Drawer 补充统一（2026-09-08）

针对用户截图中的标题留白和表单外框差异，逐项复查所有 Sheet 调用方及内嵌账户表单。统一外侧留白 24px、标题说明间隔 8px、标题区至正文 24px；标准页头不带分隔线。普通表单使用开放布局，不套整块装饰外框；输入、独立数据块及底部操作区保留有含义的边框。底部操作区使用单条顶线、16px 上内边距，按钮靠右并允许换行。保留原有滚动、草稿与业务行为。

验收须逐项记录所有 Drawer 的源码覆盖，并实测用户截图对应的创建账户和录入成交，以及不同内容结构的代表侧栏；新增防回退检查，禁止遗留页头与整块表单外框重新进入 Drawer。


本次 Drawer 补充实现已覆盖 25 个 Sheet 调用点及内嵌账户表单，12 种侧栏有实际布局证据，另有创建账户与成交录入的深色窄屏截图复验。详见检查报告“Drawer 专项复查与修复”；数据依赖入口的完整业务流程仍按原任务验收要求保留。
