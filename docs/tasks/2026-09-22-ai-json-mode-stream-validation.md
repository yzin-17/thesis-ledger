# AI 输出模式与完整流验收实施任务

> 任务标识：`2026-09-22-ai-json-mode-stream-validation`
> 对应 Spec：[AI 输出模式与完整流验收](../specs/2026-09-22-ai-json-mode-stream-validation.md)
> 状态：实施中；下面的实现与验证分别记录，不以代码提交代替测试。

## 实施清单

- [x] T0：读取当前完整适配器、共享契约、Provider 编辑和 SDK 锁定版本输出实现，建立增量 Spec/Task。
- [ ] T1：共享契约新增明确的 `json_mode`，保留 `json_validated` 的旧语义。
- [ ] T2：统一 SDK 输出策略与完整输出解析；JSON Mode 按同一生成契约校验；不支持组合在发送前拒绝。
- [ ] T3：流式用量在最终输出校验前捕获；消费输出 Promise 的失败；不改变 unknown、不自动重试。
- [ ] T4：补实际 HTTP/SSE 适配测试及共享契约回归测试，覆盖三种模式、usage 尾帧和错误路径。
- [ ] T5：输出方式文案与模式选项同步，补兼容与回滚说明。

## 验证门禁

- [ ] 静态语法与变更检查。
- [ ] 共享契约和 AI 接入层定向测试。
- [ ] 相关包 typecheck/build 与仓库边界/尺寸门禁。
- [ ] PR CI。
- [ ] 目标运行态/真实 Provider：Chat、Responses 的 JSON Mode 及流式用途生成。

当前执行环境未能通过网络克隆仓库或安装锁定依赖；远端文件通过已授权 GitHub 连接读取和更新。该限制不构成测试通过证据。未获得 CI 或实际运行结果前，PR 应保留 Draft 状态。

本阶段不包含自动能力选择。不得把它标记为已实现，也不允许通过默认开启未知能力来替代。
