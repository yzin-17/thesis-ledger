# 自然语言创建与修改回测策略实施任务

> 任务标识：natural-language-strategy-authoring  
> 日期：2026-09-18  
> 对应 Spec：[自然语言创建与修改回测策略](../specs/2026-09-18-natural-language-strategy-authoring.md)  
> 状态：文档已生成；全部实现与运行态门禁待执行。  
> 本轮交付范围：仅文档，不执行应用实现、迁移、构建、部署或 Git 提交。

## 1. 范围、责任与前置条件

本任务消费 Spec 的 NL-D-01—NL-D-06、NL-AC-01—NL-AC-16。后续实施按状态机、执行面、消费面、部署验收推进；先形成一条描述到草稿再到策略 v1 的最小纵向路径，再补齐局部修改和恢复场景。

2026-09-18 已静态核对共享 V2 Schema、实验探索、AI 审计/研究执行和策略创建入口。现有实验探索不能作为自然语言编写已实现的证据。工作树包含其他任务的 Server、Desktop、领域与文档修改；实施前记录相关输入与差异，不能覆盖或提交无关修改。

职责分工按模块定义，不代表本轮派发子代理：策略编写负责草稿/澄清/提案与编排；AI 能力负责指定模型调用和审计；策略领域负责正式版本与事务创建；Desktop 负责交互和持久化状态消费；集成负责人负责跨层证据。

### 外部依赖与交接

| 交接点 | 生产者 | 本主题消费者与条件 |
| --- | --- | --- |
| M-入口 | [已归档策略中心 Task](../archive/tasks/2026-09-17-strategy-center-ux-consolidation.md) T07 | 本主题 T06 依赖独立新建页、稳定草稿/版本导航接入点；验证手动创建仍可独立使用 |
| M-来源 | 同一 Task 的 T01/T07 | T04/T06 共同确认来源联合契约和详情扩展点；后端领域可先独立实现，不将本主题全量完成变为策略中心前置 |
| M-定义 | 本主题 T01/T03 | T04/T06/T07 只消费已验证的草稿、定义及校验契约，避免在 UI 重写规则 |
| M-执行 | 本主题 T02 | T03/T05/T06 消费持久化操作、费用和错误状态；研究任务不能误领编写任务 |

验证顺序为定向测试 → 受影响包测试与 build/typecheck → 仓库门禁 → 隔离数据库/API → 目标 Docker/在线 Provider → 浏览器。高层任务不得越过自身的失败前置；已通过检查仅在输入改变或有新疑点时重跑。

## 2. 状态机与持久化

- [ ] T00：固定能力、所有权与接口基线
  - 覆盖：NL-AC-03、NL-AC-04、NL-AC-11、NL-AC-14 的前置契约。
  - 依赖：无；开始前核对当前代码和未提交改动。
  - 范围：盘点 V2 与执行器实际支持交集、标的目录、创建版本事务、AI 执行任务类型/租约、权限与来源接口；固定首期允许的语义能力表。
  - 产物：具体模块路径、接口/错误码草案、字段所有权、M-入口/M-来源交接；明确单请求输入/输出限制、超时、补投次数及计费未知确认口径。
  - 完成条件：每种支持/不支持场景有现有契约或明确缺口；服务端可执行规则不靠模型提示词保证。确认本功能不会进入实验封存和采纳流程。
  - 验证：契约走查与代表性定义样本；涉及模块依赖时更新计划中的边界门禁范围。需改变用户决策的发现先修订 Spec。

- [ ] T01：建立编写草稿、操作与提案持久化契约
  - 覆盖：NL-AC-06、NL-AC-08、NL-AC-13、NL-AC-14。
  - 依赖：T00。
  - 范围：共享 Schema、草稿及修订、澄清问题、操作、提案与来源引用；创建、分页、读取、更新、丢弃；用户权限隔离及乐观并发。
  - 完成条件：可保存不完整内容但不得标为有效定义；同修订并发写入仅一个成功；丢弃/已保存草稿不可继续写入；跨用户不可访问。
  - 数据库：新增保留数据的 migration，不改历史 SQL；同步 matrix、inventory（如涉及 raw-owned）、runtime 输入与部署结构检查。
  - 验证：共享契约与 repository 定向测试、隔离 PostgreSQL 修订竞态/权限/迁移；记录存量数据兼容证据。

## 3. 执行面与策略转换

- [ ] T02：实现指定模型的持久化编写执行
  - 覆盖：NL-AC-11、NL-AC-12、NL-AC-13。
  - 依赖：T01。
  - 范围：分析/生成/修改操作的提交、幂等投递、领取、审计和终态读取；固定 Provider/Model，不使用研究执行回退。先完成一条待执行到成功/失败的链路。
  - 完成条件：任务种类隔离、每草稿一个活动付费操作、用量费用可追溯；状态/attempt 原子提交；队列不可用按 Spec 拒绝新调用，无请求内同步降级。
  - 验证：受控 Provider 的成功/失败/格式错误、重复提交、晚到结果、未知费用确认、越权输入；检查调用次数和实际模型。单元测试不冒充在线 Provider 证据。

- [ ] T03：实现分析、澄清与生成校验
  - 覆盖：NL-AC-02、NL-AC-03、NL-AC-04、NL-AC-05。
  - 依赖：T01、T02。
  - 范围：需求覆盖清单、标的目录确认、问题依赖及答案失效、不支持项处理、运行意图分类；生成 V2 并执行结构/能力/语义校验。
  - 完成条件：关键条件缺失或不支持项未解决不可生成；任何结果绑定输入修订；不沿用探索种子默认仓位/费用。不把模型提供的需求清单当作唯一的漏项检测证据，校验已确认答案与输出映射。
  - 验证：完整/缺失/矛盾描述；天与 K 线根数、百分比、信号和执行时点、仓位/费用、无风险规则的明确选择、Schema 合法但遗漏要求；无支持能力的要求保留原文及原因。
  - 产物：M-定义及代表性同源 fixtures，供客户端消费。

- [ ] T04：实现确认保存、来源与正式策略原子衔接
  - 覆盖：NL-AC-08、NL-AC-09、NL-AC-10、NL-AC-14。
  - 依赖：T01、T03；来源接入依赖 M-来源的契约交接。
  - 范围：由策略领域暴露稳定事务创建能力；预期修订校验、策略/v1/来源/草稿终态同事务提交；重复确认返回同一结果。
  - 完成条件：确认只保存当前审核定义，未应用提案必须先处理；保存不调用回测、实验、风险应用。来源可读且不伪造实验引用，历史未知来源不回填猜测。
  - 验证：隔离 DB 并发保存、响应丢失重试、保存期间编辑、事务失败回滚、来源读取与用户隔离；检查策略数量及相同版本引用。

- [ ] G1：受控环境的最小纵向闭环
  - 覆盖：NL-AC-02—NL-AC-06、NL-AC-09—NL-AC-11、NL-AC-13 的跨层事实。
  - 依赖：T01—T04。
  - 责任与环境：本主题集成负责人；实际 Server/执行组件、隔离 PostgreSQL、受控模型响应，不要求浏览器完成。
  - 完成条件：描述 → 分析 → 回答 → 生成 → 审核确认 → 一个策略及 v1 → 查询来源；重试确认返回同一引用。
  - 证据：真实 API 请求/响应、持久化状态与版本、调用次数、来源及校验结果。标记“受控集成”，不得宣称在线模型可用。

- [ ] T05：补齐局部修改、取消与故障恢复
  - 覆盖：NL-AC-07、NL-AC-08、NL-AC-11、NL-AC-12。
  - 依赖：G1；沿用 T01/T02 的状态和执行基础。
  - 范围：变更对象澄清、服务端允许范围、前后差异、提案确认/放弃；取消、租约超时、未投递恢复和执行结果未知处置。
  - 完成条件：范围外定义保持一致；旧提案不可应用；保存/丢弃后晚到结果不写草稿；无法判断是否付费的故障不自动重试。取消及失败费用按真实记录显示。
  - 验证：只改止损但模型改仓位/执行时机、隐式连带修改、空变更、手动编辑导致失效、重复确认、进程重启、队列查询失败、最大补投次数、Provider 响应丢失与晚到提交。

## 4. 消费面与交互

- [ ] T06：新建策略入口与草稿工作页
  - 覆盖：NL-AC-01、NL-AC-05、NL-AC-06、NL-AC-10、NL-AC-16。
  - 依赖：M-入口、M-来源、G1。
  - 范围：手动/自然语言切换、恢复草稿列表及分页、输入/规则双栏、窄屏布局、自动保存状态、读取操作进度、最终保存与来源详情。
  - 完成条件：只在确认后进入正式策略列表并定位实际 v1；已保存草稿进入源版本；保存后回测仍由用户启动。保留运行意图供回测配置确认，不自动提交。
  - 数据消费：TanStack Query 管理草稿/修订/操作/版本读取、错误与竞态；终态重新取完整结果，恢复读取不依赖页面一直打开。
  - 验证：路由级集成、切换输入保留、刷新/直接打开、保存失败/离线、并发冲突、正在保存时离开、只读来源、来源类型降级。复用现有组件，开始 UI 实现时按项目要求读取 shadcn skill。

- [ ] T07：澄清卡片、规则审阅与局部修改确认
  - 覆盖：NL-AC-02—NL-AC-08、NL-AC-11、NL-AC-16。
  - 依赖：T03、T05、T06。
  - 范围：已理解/待确认/不支持/回测时设置、目录选标的、依赖问题失效；实际定义可读预览、结构化编辑；修改建议前后差异与确认应用；模型/费用/错误提示。
  - 完成条件：所有动作与服务端校验原因一致；客户端不自行授予修改范围；待确认与过期状态不能误点保存。采用页内审核区，重要规则不埋在长弹窗。
  - 验证：缺失/矛盾/不支持卡片、模型不可用、未知费用确认、局部修改/放弃/过期、手动编辑后规则同步、键盘与主题、窄屏及标签交互。

## 5. 部署与真实验收

- [ ] G2：目标运行态、在线 Provider 与真实回测
  - 覆盖：NL-AC-09—NL-AC-15 的真实执行证据。
  - 依赖：T01—T07 定向验证、受影响包 build/typecheck、仓库门禁及隔离 DB 通过。
  - 责任与环境：本主题集成负责人；实际配置的 Provider、目标 Server/Worker/PostgreSQL/队列和市场数据。仅确认配置状态，不输出凭证。
  - 更新入口：本功能预计新增数据库及运行时结构，使用相邻 infra 的 `./scripts/update.sh thesis-ledger`；执行前按实际输入复核，不绕过标准入口替换容器。后续兼容纯代码变更才考虑通过预检的 sync-code。
  - 完成条件：在线模型完成自然语言到 v1，运行与来源可刷新读取；用户明确发起回测后检查数据完整性、成交、拒单、警告与权益曲线。另验服务重启、模型超时/结果未知、队列不可用；复用同一镜像及环境进行故障注入。
  - 证据：构建输入、镜像/运行版本、结构检查、脱敏操作/版本/回测引用、Provider 用量与故障恢复终态。生成成功、回测成功与策略表现分别记录；外部不可用时保留未通过状态。

- [ ] G3：真实浏览器交互验收
  - 覆盖：NL-AC-01—NL-AC-12、NL-AC-16 的用户可见行为。
  - 依赖：T06、T07、G2；目标前端资源与源码一致。
  - 责任与入口：本主题集成负责人；策略中心 → 新建策略 → 自然语言创建。
  - 完成条件：完整描述和缺项描述均走通；草稿离开/恢复、局部修改/拒绝、失败重试、并发冲突、保存后实际版本及用户回测全部可操作。测试宽/窄屏、明暗主题、键盘焦点、错误/空/加载态与控制台。
  - 证据：关键页面截图、用户动作与结果引用；不得用 TypeScript、组件测试或文字线框替代。关键外部条件未满足时不勾选。

- [ ] T08：最终一致性审查与文档收口
  - 覆盖：全部 NL-AC；不新增业务功能。
  - 依赖：T00—T07、G1—G3；若真实门禁未完成，只能记录阶段结论，不能标记本项完成。
  - 范围：核对 Spec、Schema、模块边界、页面、migration 与事实证据；核对与策略中心来源/入口交接，无循环依赖、重复业务状态和遗漏来源。
  - 完成条件：逐条 AC 可定位证据；已实现、已测试、已部署区分记录；更新导航与状态，满足项目归档条件后再归档 Task。

## 6. 验收映射

| 验收项 | 责任任务 | 集成门禁 |
| --- | --- | --- |
| NL-AC-01 / 创建入口 | T06 | G3 |
| NL-AC-02 / 分析与澄清 | T03、T07 | G1、G3 |
| NL-AC-03 / 标的与能力 | T00、T03、T07 | G1、G3 |
| NL-AC-04 / 语义保真 | T00、T03、T07 | G1、G3 |
| NL-AC-05 / 可读草稿 | T03、T06、T07 | G1、G3 |
| NL-AC-06 / 保存恢复 | T01、T06、T07 | G1、G3 |
| NL-AC-07 / 局部修改 | T05、T07 | G3 |
| NL-AC-08 / 过期并发 | T01、T04、T05、T07 | G3 |
| NL-AC-09 / 幂等入库 | T04 | G1、G2、G3 |
| NL-AC-10 / 来源后续动作 | T04、T06 | G1、G2、G3 |
| NL-AC-11 / 模型费用 | T00、T02、T05、T07 | G1、G2、G3 |
| NL-AC-12 / 执行恢复 | T02、T05、T06 | G2、G3 |
| NL-AC-13 / 权限边界 | T01、T02 | G1、G2 |
| NL-AC-14 / 兼容迁移 | T00、T01、T04 | G2 |
| NL-AC-15 / 真实闭环 | T02—T07 | G2 |
| NL-AC-16 / 浏览器可用 | T06、T07 | G3 |

## 7. 验证入口与证据记录

后续实施在责任任务中记录实际新增测试路径及定向命令；不以不存在的测试文件伪造已准备好的验证。可复用入口如下，均尚未为本功能执行：

| 层级 | 入口或要求 |
| --- | --- |
| 共享契约 | `pnpm --filter @thesis-ledger/schemas exec vitest run <本任务新增测试路径>` |
| Server | `pnpm --filter @thesis-ledger/server exec vitest run <本任务新增测试路径>`，覆盖状态和事务竞态 |
| Desktop | `pnpm --filter @thesis-ledger/desktop exec vitest run <本任务新增测试路径>`，覆盖路由/交互状态 |
| 包与仓库 | 按受影响包 manifest 执行 test/build/typecheck；核对边界、复杂度、秘密检查及迁移结构门禁 |
| 数据库/API | 隔离 PostgreSQL 上执行迁移、并发与 HTTP 集成，不能只做 mock |
| 运行态/浏览器 | G2/G3，单独记录外部依赖、输入和实际结果 |

每次验证记录日期、命令或操作、输入文件/版本范围、结果、证据位置、剩余限制。高成本检查通过后，输入未变化时复用证据。

## 8. 本轮文档交付状态

- 已形成：产品决策、逐页交互线框、草稿/提案/操作/来源契约、失败恢复、模块边界、依赖及 16 项验收标准。
- 已同步：策略中心 Spec/Task 的新建入口与来源边界、领域词汇和文档索引。
- 文档检查（2026-09-18）：新旧两组 Spec/Task 共四个文件的 Prettier `--ignore-path /dev/null --debug-check` 通过，避免被 `.prettierignore` 跳过；本轮涉及文档的 `git diff --check` 通过。只读脚本核对相对链接、源码引用与代码围栏通过，16 项 NL-AC 均有责任任务映射，9 项任务及 3 项门禁全部未勾选。
- 一致性审查：区分编写草稿与实验候选，原策略中心 AC-02 限定为实验从零探索；来源不伪造实验引用，入口/来源交接无全量实现循环依赖。上述结论仅针对文档，不构成功能测试。
- 待执行：T00—T08、G1—G3 全部保持未勾选；没有运行态或浏览器通过结论。
