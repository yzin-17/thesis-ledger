# 现金页面 UI 优化实施任务

对应 Spec：[`../specs/2026-09-03-cash-page-ui-optimization.md`](../specs/2026-09-03-cash-page-ui-optimization.md)

来源：`/Users/yzin/Downloads/2026-09-03-cash-page-ui-optimization-tasks.md`

状态：T1-T9 已完成并验证。

## 任务

- [x] T1：梳理现金页面结构与数据来源
  - 覆盖验收标准：AC1、AC3、AC4、AC5、AC6、AC9
  - 结论：入口为 `AccountDataPage` 的 `CashSection`；估值来自 Portfolio Query，事件来自账户 Ledger Query，定期计划来自现金计划 Query；现有接口足以支持 UI 改造。
  - 约束：不修改领域模型、结算规则和现金写入链路。
  - 验证证据：已完成代码、类型、Query 和现有现金操作入口审查。

- [x] T2：重构 Header 操作层级
  - 覆盖验收标准：AC2、AC8
  - 完成条件：现金入账为唯一 Primary；账户间划转为 Secondary；现金快照进入 More Menu；常见断点下入口仍可访问。
  - 涉及范围：现金页面 Header 和现有 DropdownMenu。
  - 验证方式：UI 契约测试、typecheck、浏览器断点检查。
  - 验证证据：现金入账保留唯一 Primary，账户间划转使用 outline，现金快照位于“更多现金操作”；Desktop/Mobile 浏览器均可访问。

- [x] T3：实现紧凑现金总览
  - 覆盖验收标准：AC1、AC3、AC7、AC8
  - 完成条件：展示可用现金、待结算应收/应付、各自笔数和多币种明细；不可靠汇率不合并；移除“大外层 Card + 币种 Card”嵌套。
  - 涉及范围：现金总览组件和前端纯展示组合。
  - 验证方式：单币种、多币种、无汇率、空状态测试和响应式检查。
  - 验证证据：`AccountDataCashSections` 使用三项摘要网格，按币种保留余额并仅在可靠折算值可用时展示合计；单币种空余额、真实 CNY 余额和移动端均已检查。

- [x] T4：重构待结算列表
  - 覆盖验收标准：AC4、AC7、AC8
  - 完成条件：轻量 Section/List、短日期、相对日期辅助文案、类型/币种/备注信息区、右对齐正负金额、弱化状态、按结算时间升序；不推测时间。
  - 涉及范围：待结算纯函数、列表行和金额/状态展示。
  - 验证方式：应收、应付、排序、备注、无结算时间和移动端测试。
  - 验证证据：`pendingCash` 只读取明确的 `settledAt/expectedAt`，按时间升序；金额使用 `+/-`，短日期和相对日期、备注、空状态与移动端已通过测试/浏览器检查。

- [x] T5：将已结算现金流水改为最近流水
  - 覆盖验收标准：AC5、AC7、AC8
  - 完成条件：标题改为“最近流水”，默认 5 条，提供页内查看全部，单行列表展示金额、状态、币种、时间和备注。
  - 涉及范围：现金流水读模型和展示组件。
  - 验证方式：0/1/5/>20 条流水、展开/收起和排序测试。
  - 验证证据：`settledCashFlows` 按最近时间倒序，页面默认 `rows.slice(0, 5)`，超过 5 条提供页内“查看全部/收起”；21 条流水默认 5 条组件回归、1 条真实流水和空状态已通过验证。

- [x] T6：整合定期入账与待确认
  - 覆盖验收标准：AC1、AC6、AC7、AC8、AC9
  - 完成条件：一个轻量 Section 内先待确认后计划；空状态紧凑；创建、确认、跳过、逾期恢复行为不变。
  - 涉及范围：`AccountDataRecurringCashDeposits` 展示层，不修改现金计划 API。
  - 验证方式：计划、待确认、空状态、错误和 Drawer 操作测试。
  - 验证证据：定期入账与待确认已合并为单一轻量 Section，待确认在计划之前；创建、确认、跳过、恢复、逾期提示和新建计划 Drawer 回归通过。

- [x] T7：统一现金页面视觉层级与响应式
  - 覆盖验收标准：AC1、AC2、AC3、AC4、AC5、AC7、AC8
  - 完成条件：减少完整 Card 和实心 Badge；金额优先；Section 间距统一；Light/Dark、Desktop/Tablet/Mobile 无明显溢出。
  - 涉及范围：现金页面局部 Tailwind 原子类和现有组件组合。
  - 验证方式：源码契约、build 和浏览器视觉检查。
  - 验证证据：现金页已抽出独立组件，移除待结算/最近流水重型 Card 和现金账户无关的定期模块；Desktop、390px Mobile、Light/Dark 均无横向溢出，Console 无 error/warning。

- [x] T8：补充自动化回归
  - 覆盖验收标准：AC1-AC9
  - 完成条件：覆盖总览、待结算、最近流水、定期入账、所有空/异常状态及既有现金操作。
  - 验证方式：Desktop 定向测试、相关 Server 回归、typecheck、build、`git diff --check`。
  - 验证证据：Desktop 全量 20 个测试文件、129 个测试通过；现金页定向测试 4 个文件、50 个测试通过，覆盖排序、正负金额、短日期、备注、21 条流水默认 5 条、空状态、计划状态和已有现金操作；Desktop typecheck/build 与 `git diff --check` 通过。

- [x] T9：浏览器视觉验收
  - 覆盖验收标准：AC1-AC9
  - 完成条件：验证当前截图等价场景、多币种、大量流水和移动端；记录浏览器/运行时证据，不以静态测试替代。
  - 验证方式：本地 Desktop 浏览器在关键断点下检查布局、操作和 Console。
  - 验证证据：本地 API 返回 200；真实现金账户浏览器检查确认页面顺序、现金入口、More Menu、快照 Drawer、计划 Drawer、CNY 待结算/流水展示；默认 Desktop（1280px）和 Mobile（390px）文档宽度均不超过视口，Light/Dark 检查通过，Console 无 error/warning；多币种和大量流水由纯函数/定向 fixture 覆盖，未通过浏览器写入额外财务数据。

## 依赖与阻塞

- T1 已完成；T2-T7 可在 T1 基础上实施，T8 依赖实现后的界面稳定，T9 依赖可运行的 Desktop 环境。
- 当前无 Blocking Question。
- 若本地运行时不可用，确定性测试和构建可完成，但 T9 保持未完成并记录原因。

## 最终一致性 Review

- [x] Spec 中的全部验收标准均有对应实现
- [x] 所有已勾选任务均有验证证据
- [x] 所有任务依赖均已满足且无错误阻塞关系
- [x] 跨任务接口、类型和命名保持一致（如适用）
- [x] 不存在未解决的 Blocking 问题、占位描述或未定义的实现契约
- [x] 实现未超出 Spec 声明的范围
- [x] 测试策略、测试实现与验证结果一致
- [x] 测试与文档已同步更新
- [x] 必要实施 Step 均已验证；未获提交授权，工作保持未提交
- [x] 未发现实现、Spec 与任务文档之间的不一致

### Review 结论

- 结论：T1-T9 已完成，Spec、Task 与实现一致。
- 发现的问题：无 Blocking 问题。
- 遗留风险：未在浏览器中创建额外多币种财务流水，相关边界由纯函数和 21 条组件 fixture 覆盖；生产/Compose 环境未纳入本次 UI 验收。
- 验证命令与结果：Desktop 全量测试 20 个文件/129 个测试通过；现金定向测试 4 个文件/50 个测试通过；Desktop typecheck、Vite build、浏览器桌面/移动/主题检查和 `git diff --check` 通过。
