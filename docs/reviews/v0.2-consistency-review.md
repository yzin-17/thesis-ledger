# V0.2 一致性 Review

已检查 Provider Registry、Market Adapter、MarketBar 落库、缓存、DataQuality、ProviderHealth 和回填路径。业务层只通过 `MarketService`/Contract 读取行情；Quote/Bar/Indicator/Chip 保留 provider、时间和 stale/partial 语义；主源失败、限流和熔断会进入 fallback 或明确失败。`pnpm --filter @investment-os/dsa-adapter test`、server/domain test 与 build 已通过。

`pnpm run provider:failover -- --allow-service-stop` 已在真实 Compose PostgreSQL/Redis/DSA 环境执行：脚本先预热 Quote，再清理 fresh cache，停止 DSA，连续三次健康检查进入 `degraded → degraded → down`，Quote 从 last-valid cache 返回 `stale=true`，写入 DataQuality issue，恢复 DSA 后回到 `healthy`。外部 DSA Fork 与真实 Provider 仍属于发布前独立门禁。
