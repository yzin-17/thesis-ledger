# V0.1 一致性 Review

核心闭环已按 Account → Ledger/Position → Portfolio → Risk → Notification → AI 的边界检查。截图导入先保存 Draft，提交时生成可审计的 Ledger Adjustment；Portfolio、Risk 和 AI 只通过 Server 服务或只读 Tool 获取数据，没有客户端直连 DSA 或模型 SDK 的路径。截图伪 MIME、字段缺失、重复提交、回滚、账户约束和 AI 权限测试均已通过。

Desktop 生产包、Mobile 真机、真实 DSA、飞书 Webhook 和浏览器人工流程需要外部运行环境，继续列为发布前检查项，不把本地 build/test 结果当作端到端人工验收。
