# V0.5 一致性 Review

Strategy Schema、Backtest Contract、成本模型、A 股交易约束、PIT/完整性检查、样本内外切分、Walk Forward、Benchmark 和 QuantStats period metrics 已统一到可复现的 Backtest Worker 输入输出。回测结果保存策略版本、数据与引擎元数据、成本和限制；AI 只能提交同一 Strategy Schema 的研究任务，不能绕过 Worker 或生成执行订单。

Domain 回归、Schema 契约、Server Worker 测试和 Desktop build 已通过。Strategy/Backtest UI 的人工创建→运行→结果验收，以及真实 Worker/数据集的随机重跑仍需浏览器或部署环境确认。
