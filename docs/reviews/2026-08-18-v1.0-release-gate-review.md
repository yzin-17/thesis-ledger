# V1.0 发布门禁 Review

## 已完成的本地门禁

- Prisma Client 已按当前 schema 生成，15 个迁移目录通过静态矩阵检查。
- 全包 `build`、`typecheck`、`lint`、`format`、Schema/Domain/Server/Adapter 测试、Desktop build、基准集检查和 Secret 扫描均通过。
- 已具备备份/恢复脚本、数据导出、指标、健康历史、结构化脱敏日志、错误追踪接入点、API 限流和固定回归基准集。
- Desktop 页面已完成导航与状态组件的代码级 smoke；Risk、Performance、Strategy、AI、Journal、Provider/Automation 页面均接入 loading/error/empty 等状态。本轮通过开发服务浏览器实际点击完成 8 个主导航页面的标题核对，在 800/520px 视口检查响应式布局，并在空组合状态核对四步首次运行引导及 mock 账户/持仓录入后的即时恢复态；同时修复异步表单 `event.currentTarget` 失效导致保存后不刷新的问题。另在 macOS DMG mock API 中观察到 empty/error/stale/ready 四态，并用 pending fixture 观察到 loading；这些属于开发/mock 证据，不替代真实 API 的 release 包验收。
- 真实 Compose API 链路已通过账户、交易、Portfolio/Risk、Feishu、AI provenance、Backtest 和 integrity smoke；浏览器视觉操作与 release 包安装启动仍由目标环境验收。
- 真实 Compose 故障注入脚本已验证：预热 Quote 后停止 DSA，连续三次健康检查由 `degraded → degraded → down`，Quote 从 last-valid cache 返回 `stale=true`，DataQuality 记录 open issue；恢复 DSA 后回到 healthy，Provider health history 保留证据。
- `pnpm run db:integration` 已在真实 Compose PostgreSQL/Redis 上通过迁移运行态、integrity、Redis NX 锁互斥和 Snapshot 幂等；另将 custom-format 备份恢复到隔离数据库，核对 3 个账户、1 条 Ledger、4 个 Snapshot 和 15 条迁移记录后清理临时库。
- 本轮收尾补齐移动端 Expo/React Native runtime：复用 `@investment-os/api-client`，提供 Portfolio/Risk 导航、Decimal 归一化和 loading/empty/error/stale 状态；依赖已与 Expo SDK 57 对齐，`typecheck`、`build`、`test` 与 web export 通过，Android arm64 release APK/AAB 已生成并完成静态包信息核对，最新 APK 已安装到 API 35 ATD 模拟器并完成 Portfolio/Risk 与状态检查；现已加入 `keystore.properties`/`ANDROID_*` release signing 入口，但本地默认产物仍为 debug signing。iOS CocoaPods 已成功集成并生成 `Podfile.lock`。本机已删除旧 iOS 26.0.1 runtime 并安装、验证官方 iOS 26.3.1 runtime；workspace Release build 已成功，`com.investmentos.mobile` 已安装并启动到 iPhone 16e，Portfolio ready/error 运行证据见 `docs/reviews/evidence/mobile-ios/2026-08-18-ios-release-e2e.md`。
- 本轮还尝试临时释放 NDK 空间安装 Android 35/30 arm64 系统镜像；Android 35 emulator 要求约 7.3 GiB userdata，Android 30 镜像占用约 3.1 GiB，均因宿主机空间不足无法启动。镜像、AVD 和临时构建目录已清理，NDK 已恢复原路径；这次尝试未改变 Mobile 设备门禁结论。
- Desktop 空组合页新增四步 First-run Onboarding（账户、手动/截图导入、数据源/Feishu、风险限制），并为 Portfolio、Market、Risk、Import、Performance、Strategy、AI、Provider 复用状态提示；Desktop UI 契约测试和生产静态构建通过。
- `pnpm licenses:scan` 已生成 671 个依赖版本的许可证清单，未发现缺失许可证字段；DSA Fork 与移植代码 attribution 仍需人工核对。
- `pnpm v1:e2e` 已在临时 release-candidate Compose Server 和隔离 PostgreSQL 上通过完整核心链路，包含真实 DSA Quote、测试 Feishu HTTP 投递、AI Tool provenance、Backtest 和末尾 integrity；报告见 `docs/reviews/2026-08-18-v1-core-e2e-report.md`。
- `pnpm disaster:recovery` 已完成 custom dump/校验/隔离恢复；同一轮 DSA failover 与 Feishu 503→retrying→delivered 证据已归档，见 `docs/reviews/2026-08-18-v1-disaster-recovery-report.md`。
- `pnpm accessibility:check` 已通过 13 项 Desktop/Mobile 代码级语义、键盘焦点、响应式断点、release 窗口最小尺寸、reduced-motion 和 WCAG AA 对比度检查；本轮开发服务浏览器实测首个导航按钮获得 2px `focus-visible` 外框，800/520px 视口均保留主导航与 Portfolio 内容；本版本不纳入高对比度模式优化，设备与 Desktop release 包视觉人工项仍在 [Accessibility/Responsive checklist](2026-08-18-v1-accessibility-responsive-checklist.md) 中保留。
- Electron production build 已生成未签名 macOS DMG 与 Windows NSIS EXE；DMG `hdiutil verify` 通过，并完成 macOS DMG 挂载、首次启动、账户/持仓/Portfolio mock API 烟测。随后在真实 Compose API 上完成 macOS release 的 Portfolio、Import、Risk、Strategy/Backtest、Journal、AI 和 Provider/Automation 页面烟测，证据见 `docs/reviews/evidence/desktop-macos/2026-08-18-compose-release-e2e.md`；签名、Windows 目标平台安装启动和全量 D1-D10 仍未完成，详见 `docs/reviews/2026-08-18-desktop-production-build.md`。
- 已补充可交接的 [Desktop UI E2E 清单](2026-08-18-v1-desktop-e2e-checklist.md) 与 [Mobile UI E2E 清单](2026-08-18-v1-mobile-e2e-checklist.md)，包含真实 API、五类状态、键盘/小屏和跨端对照的证据字段；目标平台执行前不将 checklist 当作验收结果。
- DSA Fork Delta/Upstream Merge Playbook 已完成，当前 `origin/main` 与 `upstream/main` 零差异，详见 `docs/reviews/2026-08-18-dsa-fork-delta-audit.md`。
- 本轮最终自动化复核再次通过：`pnpm test`（全包测试套件通过）、`pnpm typecheck`、`pnpm lint`（含 build 与 import boundary）、`pnpm format`、`git diff --check` 和 `pnpm accessibility:check`；Docker Desktop 重启并完成非破坏性空间清理后，Compose `/health`、`/integrity`、数据库/Redis 集成、DSA failover、V1 核心 E2E 和灾备恢复均重新通过。
- 随后再次尝试重启 Docker Desktop 时，helper 进程未能在超时内退出，当前 Docker CLI 暂不能连接 daemon；未删除数据卷，也未将这次环境故障倒写为业务测试失败。上条 Compose 通过证据仍对应已完成的那一轮运行态复核，后续发布前需在 Docker 恢复后再做一次短 smoke。
- 2026-08-01 又仅启动现有 Docker Desktop 应用并复核 `docker compose ps`，仍返回 `Docker Desktop is unable to start`；本次没有删除卷、清理数据或修改 Compose 配置。
- 同日读取 Docker Desktop host 日志确认环境根因：`com.docker.virtualization.log` 在 VM 关闭前记录 `write .../log/vm/console.log: no space left on device`；因此当前阻塞是 Docker VM 磁盘空间/启动状态问题，而不是 Compose 配置或业务测试失败。未执行删除数据卷等破坏性清理。
- 清理本轮可恢复的临时 Android/JDK/Gradle 构建缓存并再次启动 Docker Desktop 后，`docker compose ps` 仍返回同一错误；最新 host 日志仍保留上述 `no space left on device` 记录，未删除 Docker 数据卷。
- 2026-08-01 后续在空间恢复后安装 Android 35 AOSP ATD arm64 系统镜像并启动临时 emulator；当前源码的 release APK/AAB 已重新构建，APK 已安装并通过 Portfolio/Risk、loading/empty/error/stale 及 600×1000 小屏滚动检查。随后挂载 macOS DMG，以同一隔离 `fixture-account` 完成 Desktop/Mobile 数值、Risk 和 ready/stale/error 对照；另在全新临时数据库中用 macOS release 完成账户、手动持仓和风险规则 onboarding。iOS 26.3.1 workspace build 和 Simulator ready/error 运行态也已归档，最新产物 SHA-256 与 XML/PNG/对照报告已归档至 `docs/reviews/2026-08-18-mobile-runtime-review.md`、`docs/reviews/evidence/mobile-android/`、`docs/reviews/evidence/mobile-ios/` 和 `docs/reviews/evidence/desktop-macos/`。
- 2026-08-01 Docker Desktop 恢复后再次执行 `pnpm db:integration`、`pnpm v1:e2e`、`pnpm provider:failover --allow-service-stop` 和 `pnpm disaster:recovery`，均通过；本轮真实 Compose 运行态已恢复，前述 Docker 空间故障不再是当前门禁。

## 发布前阻塞项

以下项目仍不能由本地服务链路替代：Desktop 签名安装启动、Desktop 全量 UI E2E、全新数据库 onboarding 的真实 Feishu/Data Provider 配置、DSA/移植代码 attribution 人工核对和 V1 tag/release。Android、iOS Simulator、Compose 运行态、Desktop/Mobile 五态状态矩阵、T278 可访问性/响应式证据和 macOS release 的主要页面烟测已完成，但不替代 iOS 真机/TestFlight、Windows、全量 D1-D10、键盘目标环境和签名发布门禁；本版本不纳入高对比度模式优化。对应任务继续保持未勾选，并已在 `docs/operations/2026-08-18-release-checklist.md` 与 `docs/architecture/2026-08-18-spec-traceability.md` 说明责任边界。

当前任务文档共 294 项，其中 290 项已完成，以下 4 项仍保留为发布前门禁：T272、T277、T283、T294。T276 的 Desktop release 状态矩阵、T278 的键盘/焦点/响应式证据和 Mobile 五态证据已补齐；剩余项目分别需要 Desktop 目标平台安装/签名、真实新用户与 Feishu/Data Provider 配置、Windows/完整 Desktop UI E2E 和最终 tag/release；本版本不纳入高对比度模式优化；iOS Simulator 的本地 release 门禁已由 T275 证据满足，但不等同于真机/TestFlight 发布。

## 外部门禁记录

截至 2026-08-01，以下条件在当前 macOS 工作区不可替代，已记录并停止重复尝试：

| 任务 | 缺失条件                                                    | 已完成的本地准备                                                                                        | 后续动作                                                    |
| ---- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| T272 | Windows 主机/runner 的安装启动、签名证书和跨平台基础 E2E    | 已生成未签名 DMG/NSIS；`.github/workflows/ci.yml` 已提供 macOS/Windows installer job 和签名 secret 入口 | Release owner 在 Windows runner 安装、签名并保存 smoke 证据 |
| T277 | 全新目标数据库中的真实 Feishu/Data Provider 凭据            | 四步 onboarding、Provider 密码输入、提交清空和“已配置”状态已实现并用 fixture 验证                       | 提供真实凭据并在全新数据库执行一次 onboarding               |
| T283 | Windows/目标环境的 Desktop E2E 证据                         | macOS release 已归档 D1-D10 对应路径、真实 Compose 页面和状态矩阵                                       | 在 Windows/目标环境复跑清单并归档报告                       |
| T294 | 最终签名、clean-environment smoke、tag/release 和负责人批准 | 产物静态校验、测试、文档、限制说明和发布清单已完成                                                      | Release owner 完成批准后再签名、打 tag、发布                |

本记录不是失败重试的邀请；在上述外部条件未变化前，不再反复执行相同的本机检查。
