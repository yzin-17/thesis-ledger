# V0.6 一致性 Review

Provider/Prompt/Tool/Agent/Provenance/Decision Log 使用独立契约；AI 权限只允许读取和研究回测，Portfolio Gate 与 `validateGroundedAnalysis` 会拒绝无来源数字、关键数据缺失和执行指令。静态搜索确认 AI 服务没有 Ledger、Position 或 Order 写入口；server/schema/desktop build 与 AI 回归已通过。

真实模型连接和人工 Chat UI 验收保持为外部检查项。
