# ThesisLedger 领域词汇

ThesisLedger 用统一账户、Ledger 与持仓投影组织个人投资数据，并明确区分资产归属、数据录入和来源追踪。

## 账户与持仓

**资产（Asset）**：
可被账户持有和估值的标准化金融对象，以全局唯一代码标识。场内证券使用交易所后缀，场外基金使用 `.OF` 后缀。
_避免_：持仓、账户资产余额

**资产身份状态（Asset Identity Status）**：
说明资产代码、名称和类型由 Provider 验证、由用户确认，还是仍未解决。未解决资产不能写入 Ledger；用户确认的资产必须保留人工来源，不能被静默映射为另一标的。
_避免_：行情新鲜度、持仓状态

**投资账户（Account）**：
承载一组资产、Ledger 事件和持仓投影的逻辑容器，以稳定 ID 标识。名称可以重复；名称、机构、类型、模式和币种只描述账户本身。
_避免_：账户来源、导入账户

**机构（Institution）**：
资产实际所在的可选外部机构或平台，例如券商、支付宝或同花顺。机构是账户描述，不表示数据如何进入系统。
_避免_：来源、截图来源、录入方式

**账户类型（Account Type）**：
说明账户的业务容器类别，取值为证券、基金或现金。证券账户可承载场内证券和现金，基金账户可承载场外基金和现金，现金账户只承载现金；账户类型不表达数据真实性或模拟用途。
_避免_：资产类型、影子账户、账户模式

**账户模式（Account Mode）**：
说明账户代表真实资产还是用于模拟研究，取值为真实或影子。影子账户拥有独立数据但不代表真实资产；账户模式不限制账户承载的资产类别。
_避免_：账户类型、资产类型

**持仓（Position）**：
某个投资账户下单一资产的当前数量与成本状态，是 Ledger 的可重建投影。
_避免_：资产事实、交易记录

**持仓目标（Position Target）**：
风险规则所指向的某个账户中的某个资产，由 `accountId + symbol` 唯一确定；它不是新的持仓事实，也不替代 Position。
_避免_：账户资产关系表、全局标的规则

**持仓生命周期（Position Lifecycle）**：
同一 Position ID 从建立到清仓的一段持有周期；清仓后重新建仓即开始新的生命周期，即使账户和资产代码相同。
_避免_：持仓更新时间、交易批次

**观察峰值（Observed Peak）**：
风险扫描实际观察到的某个持仓目标在当前模式下的最高价格；第一版不声称它等同于完整日内行情的真实最高价。
_避免_：成本价、行情 Bar 高点

**下穿状态（Breach State）**：
某条风险规则是否已经进入触发区间且尚未恢复；它按规则、持仓目标和模式独立保存，不与观察峰值混为一体。
_避免_：风险事件、通知冷却

**全局证券规则（Global Security Rule）**：
只绑定 symbol 而不绑定账户的证券观察规则；同一价格事实按 symbol 生成一条事件，并在上下文中记录受影响账户。
_避免_：账户持仓规则、组合规则

**Ledger 事件（Ledger Event）**：
记录资产、现金、费用、税费和调整变化的不可变事实。持仓和组合数据由 Ledger 事件计算或重建。
_避免_：持仓快照、可覆盖记录

**现金余额（Cash Balance）**：
某个账户在指定币种下的当前现金金额，由 Ledger 事件计算，不表现为虚构的资产持仓。
_避免_：现金 Position、现金证券

**配置类别（Allocation Category）**：
收益分析配置视图使用的分类桶，用于比较当前配置与目标配置。当前支持股票、ETF、基金、指数和现金五类；它是分析语义，不等同于 `AssetType`、Asset 或 Position，也不改变持仓录入和标的目录的边界。
_避免_：把配置类别当成可录入的持仓类型、把指数目标当成指数 Position

**配置视图现金（Cash Allocation View）**：
收益分析中的现金配置类别，是同一分析范围内 Cash Balance 的只读投影。它参与配置金额和权重分母，但不会写入 Position，也不会生成现金 Asset。
_避免_：现金 Position、现金证券、将现金余额重复计入证券市值

**基金净值（Fund NAV）**：
场外基金在指定净值日期的单位价值，并携带数据来源和新鲜度。基金净值不是证券实时报价。
_避免_：Quote、成交价

**持仓行情详情（Market Detail）**：
从 Position 的本地持仓上下文进入的只读市场详情，不改变 Position、Asset 或 Ledger。行情详情通过 Server 共享读模型按资产能力返回 quote、日线、技术指标、筹码摘要或基金净值。
_避免_：交易执行、持仓写入、客户端直连 Provider

**行情能力分段（Market Detail Section）**：
共享读模型中的独立数据模块，状态必须区分 ready、stale、empty、unsupported 和 unavailable。客户端的 loading 只表示界面请求状态，不属于 Server 数据契约。
_避免_：把 Provider 失败伪装成空数据、把不支持能力当作请求失败

## 持仓录入

**持仓录入（Position Entry）**：
为选定投资账户建立或修正当前持仓数据的任务。手动录入与截图导入是两种并列方式。
_避免_：账户创建流程、三步导入

**录入方式（Entry Method）**：
说明本次持仓数据通过手动输入还是截图审核进入系统的来源信息。录入方式属于持仓变化及其审计记录，不属于投资账户。
_避免_：账户来源

**导入草稿（Import Draft）**：
截图识别后、正式提交前的可审核数据集合，并记录目标账户在创建或重新基线时的 Ledger 状态。草稿不会直接改变 Ledger 或持仓。
_避免_：已导入持仓、OCR 结果直写

**草稿基线（Draft Baseline）**：
导入草稿用于检测并发变化和确定回滚起点的 Ledger 状态。重新基线表示用户已审核当前状态与草稿之间的差异，不表示自动合并。
_避免_：自动合并结果、最新持仓缓存

**截图来源（Screenshot Source）**：
截图内容所属或识别出的外部平台，以可扩展的稳定代码表示，例如支付宝、同花顺、券商、银行、基金平台或未知。截图来源属于导入草稿及其置信度信息。
_避免_：账户来源、机构
